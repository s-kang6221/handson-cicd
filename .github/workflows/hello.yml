name: Hello # ワークフロー名
on: push # プッシュ時に起動するイベント
jobs: # 実行する処理を列挙するキー
  hello: # ジョブのID
    runs-on: ubuntu-latest # 処理を実行するランナー
    steps: # 処理内容
      - run: echo "Hello, world" 
      - uses: actions/checkout@v4